= render 'template/header'
.SearchItems
  .Main
    .MainContainer
      .MainContainer__header
        %p.result 
          = params[:keyword]
        %h2 の検索結果
      .MainContainer__field
        - @items.each do |item|
          = render 'item', item: item
= render 'template/footer'